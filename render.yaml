# VeriBuy Render Blueprint
# Deploy: https://render.com/docs/blueprint-spec
#
# All resources deploy to Frankfurt for lower latency to UK users.
# Private networking: services communicate via http://<name>:10000.

services:
  # ─── Backend API ─────────────────────────────────────────
  - type: web
    name: veribuy-backend
    runtime: docker
    plan: starter
    region: frankfurt
    dockerfilePath: backend/Dockerfile
    dockerContext: .
    healthCheckPath: /health
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: veribuy-postgres
          property: connectionString
      - key: REDIS_URL
        fromService:
          type: keyvalue
          name: veribuy-redis
          property: connectionString
      - key: REDIS_TLS
        value: 'true'
      - fromGroup: veribuy-secrets
      - key: STRIPE_SECRET_KEY
        sync: false
      - key: STRIPE_WEBHOOK_SECRET
        sync: false
      - key: RESEND_API_KEY
        sync: false
      - key: CLOUDINARY_CLOUD_NAME
        sync: false
      - key: CLOUDINARY_API_KEY
        sync: false
      - key: CLOUDINARY_API_SECRET
        sync: false
      - key: IMEI_CHECK_API_KEY
        sync: false
      - key: IFREE_ICLOUD_API_KEY
        sync: false

  # ─── Frontend ────────────────────────────────────────────
  - type: web
    name: veribuy-frontend
    runtime: docker
    plan: starter
    region: frankfurt
    dockerfilePath: frontend/Dockerfile
    dockerContext: .
    healthCheckPath: /
    envVars:
      - key: NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY
        sync: false
      - fromGroup: veribuy-secrets

  # ─── Redis (Cache) ───────────────────────────────────────
  - type: keyvalue
    name: veribuy-redis
    plan: free
    region: frankfurt
    ipAllowList: []
    maxmemoryPolicy: allkeys-lru
    persistenceMode: off

databases:
  # basic-256mb (~$6/mo). Free tier exists but expires in 30 days and deletes data.
  - name: veribuy-postgres
    postgresMajorVersion: '17'
    plan: basic-256mb
    region: frankfurt
    databaseName: veribuy
    ipAllowList: []

envVarGroups:
  - name: veribuy-secrets
    envVars:
      - key: JWT_SECRET
        generateValue: true
      - key: JWT_REFRESH_SECRET
        generateValue: true
      - key: INTERNAL_SERVICE_SECRET
        generateValue: true
      - key: INTERNAL_SERVICE_TOKEN
        generateValue: true
