{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile.evidence",
    "watchPatterns": ["services/evidence-service/**", "packages/**", "pnpm-lock.yaml"]
  },
  "deploy": {
    "startCommand": "export DIRECT_DATABASE_URL=${DIRECT_DATABASE_URL:-$DATABASE_URL} && node_modules/.bin/prisma migrate deploy --schema=prisma/schema.prisma && node dist/main",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
